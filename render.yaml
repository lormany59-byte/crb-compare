services:
  - type: web
    name: crb-compare
    env: python
    plan: free
    buildCommand: "cd crb_compare && pip install -r requirements.txt"
    startCommand: "cd crb_compare && gunicorn --bind 0.0.0.0:$PORT app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.10
